"use client";

/**
 * InputForm — Text/YouTube URL input with validation and submit.
 * Detects YouTube URLs and shows preview. Manages loading state.
 * TODO (Wave 5): Implement YouTube URL detection, preview thumbnail,
 * connect to startMeeting API.
 */

export function InputForm() {
  return (
    <div className="rounded-lg border border-dashed p-8 text-center text-muted-foreground">
      InputForm — stub
    </div>
  );
}
